title "Testes do comando case";
variables
    int x;
    int y;
end variables
commands
    case (10) of
        (10) : print("O resultado é bom");
    end
    x:= 10;

    while (x > 0) do
        x := (x - 1);
    end

